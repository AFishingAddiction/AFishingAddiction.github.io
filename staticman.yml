---
comments:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "comment on {options.slug}"
  filename: "entry-{@timestamp}"
  format: "yml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
  moderation: true
  name: "afishingaddiction.com"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "message"]
